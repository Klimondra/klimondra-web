import React from 'react';
import "./LoadAndAuth.css"

const Loading = () => {
    return (
        <main>
            <div className="loading">
                Běžím...
            </div>
        </main>
    );
};

export default Loading;
